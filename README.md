# Arch development image

An Arch Linux docker image with development-related programs pre-installed (as listed in
https://github.com/jsspencer/james.sls).

To build:

```
$ docker build -t arch:latest .
```

To update:

```
$ ./arch-docker-update
```
